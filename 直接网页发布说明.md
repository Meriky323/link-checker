# 直接做成网页：最简发布方式

这个工具已经是一个网页应用，不是 Excel 宏，也不是本地页面。

要让所有人直接通过一个网址访问，需要把它放到一台服务器或云平台上运行。

## 最推荐：交给 IT 部署

把整个文件夹发给 IT，然后告诉 IT：

```text
这是一个 Python Web App。
启动命令：python link_checker_app.py
监听端口：8765
环境变量：HOST=0.0.0.0，PORT=8765
```

部署后，IT 可以给你一个内网网址，例如：

```text
http://patpat-link-checker.company.local
```

或者：

```text
http://10.xx.xx.xx:8765/
```

## 如果公司支持 Docker

IT 只需要在这个文件夹里执行：

```bash
docker compose up -d --build
```

然后访问：

```text
http://服务器IP:8765/
```

## 如果要变成更像正式网页的网址

让 IT 做一个域名转发，例如：

```text
http://link-checker.patpat.local
```

转发到：

```text
http://服务器IP:8765/
```

## 注意

这个工具涉及物料表、转链、折扣码，建议放公司内网，不建议裸放公网。
